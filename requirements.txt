pytest

STEP 6: Initialize Git

git init
git add .
git commit -m "initial setup"

---

STEP 7: Connect to GitHub

git remote add origin https://github.com/your-username/mlops5.git
git branch -M main
git push -u origin main

---

STEP 8: Trigger Pipeline

git add .
git commit -m "run pipeline"
git push origin main

---

STEP 9: View Output

1. Open GitHub repository
2. Click "Actions" tab
3. Click latest workflow run
4. Click "build-and-test"
5. Click "Run App"

OUTPUT WILL BE:

Prediction: setosa
Test Passed ✅

---

FINAL FLOW

Write Code → Push → CI/CD Runs → View Output in Actions Tab

---
practical 6
MLOps Project 6 - Model A/B Testing (Steps + Commands)

1. Create project folder
   mkdir mlops-project6
   cd mlops-project6

2. (Optional) Create virtual environment
   python -m venv venv
   venv\Scripts\activate

3. Install dependencies
   pip install fastapi uvicorn scikit-learn pandas joblib

4. Create project structure
   mkdir models
   type nul > app.py
   type nul > router.py
   type nul > metrics.py
   type nul > create_models.py

5. Train and save models
   python create_models.py

6. Add routing logic (router.py)

# (code: A/B traffic split using random)

7. Add metrics logging (metrics.py)

# (code: write latency + model version to CSV)

8. Build FastAPI app (app.py)

# (code: load models + /predict endpoint)

9. Run the server
   uvicorn app:app --reload

10. Open API in browser
    http://127.0.0.1:8000/docs
